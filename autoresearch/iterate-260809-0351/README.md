# Confirmation loop: E70 replication and ICRA integration

Started 2026-08-09 after the preregistered seed-0 E70 content gate passed and the previous
25-iteration loop closed.  This loop freezes the E70 recipe and endpoints.  Its bounded objective
is to run training seeds 1 and 2, aggregate paired uncertainty without tuning, integrate the
confirmed result into the anonymous eight-page ICRA manuscript, and verify the resulting artifact.

Primary metric: ambiguity-start completion for SNMR minus absolute time, clustered by the 69
reference-only frame pairs and stratified by training seed.  Required diagnostic: SNMR minus
matched-phase shuffled latent.  No threshold, data pair, checkpoint rule, or model setting may be
changed in this loop.

Maximum iterations: 25.

Current state (2026-08-09): seed 0 and seed 1 are complete.  Seed 2 explicit, SNMR, and
absolute-time are complete; the interrupted proprioceptive cell is quarantined and must restart
from round 0, followed by shuffled-latent and the frozen analyzer.  A detached recovery supervisor
is waiting for the preregistered 26000-MiB capacity gate.  A second supervisor will bind only the
final three-seed analyzer output into the paper and provenance-checked video once those prerequisites
exist.  Until then, the manuscript intentionally renders the labeled seed-0 fallback rather than a
provisional aggregate.
