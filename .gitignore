# experiment artifacts — large binaries stay out of git; logs/metrics stay in
runs/**/ckpt*.pt
runs/**/*.pt
exports/*.npz
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
# scratch training logs at repo root (per-run logs live in runs/<name>/)
runs_*.log

# LAFAN1-derived fixture: kept local, NOT published (Ubisoft license unverified)
tests/fixtures/pair_g1_*.npz

# WBT validation NPZs (large, regenerable)
runs/wbt_validation/**/*.npz
runs/wbt_validation/gate2a_smoke/*.log

# WBT latent exports and raw policy logs (large, regenerable)
runs/wbt_latent/*.npz
runs/wbt_latent_gmr/*.npz
runs/wbt_pilot/pilot_*.log
runs/wbt_pilot_replication/pilot_*.log
runs/wbt_independent_eval/pilot_*.log
runs/wbt_horizon_calibration/*.train.log
runs/wbt_horizon_calibration/*.eval.log
